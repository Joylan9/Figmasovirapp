
  # Sovir app

  This is a code bundle for Sovir app. The original project is available at https://www.figma.com/design/Xcb9qevdzQSwzOje1YL1BB/Sovir-app.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  